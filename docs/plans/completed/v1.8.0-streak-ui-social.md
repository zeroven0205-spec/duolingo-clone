# v1.8.0 — Streak UI + Social Features

**Status:** Active
**Target Score:** 7.5 → 8.0/10
**Created:** 2026-07-02

---

## 背景

已完成 v1.2.0-v1.7.0，v1.8.0 聚焦：
1. 签到奖励 UI 接入（updateStreakAndClaimRewards 未在页面调用）
2. 好友系统 + 社交化
3. 数据库迁移（遗留）
4. 收尾遗留问题

---

## P0 — 签到奖励 UI 接入

### LING-P0-STREAK-UI · 签到逻辑接入 Learn 页面

**现状**: `updateStreakAndClaimRewards` 已实现，但从未被调用

**修复**: 创建客户端 toast 组件 + 修改 Learn 页面

**步骤 1**: 创建 `components/streak-toast.tsx`
```typescript
// components/streak-toast.tsx
"use client";

import { useEffect } from "react";
import { toast } from "sonner";

interface StreakResult {
  streak: number;
  rewardClaimed: string | null;
  isNewDay: boolean;
}

export function StreakToast({ streakResult }: { streakResult: StreakResult | null }) {
  useEffect(() => {
    if (streakResult?.rewardClaimed) {
      toast(streakResult.rewardClaimed, { icon: "🎁" });
    }
  }, [streakResult]);

  return null;
}
```

**步骤 2**: 修改 `app/(main)/learn/page.tsx`
```typescript
// app/(main)/learn/page.tsx
import { updateStreakAndClaimRewards } from "@/actions/user-streak";
import { StreakToast } from "@/components/streak-toast";

export default async function LearnPage() {
  const streakResult = await updateStreakAndClaimRewards();

  return (
    <>
      <StreakToast streakResult={streakResult} />
      {/* 现有页面内容 */}
    </>
  );
}
```

### LING-P0-STREAK-SHOW · 显示 Streak 状态

**修复**: 在 Header 组件添加 StreakBadge

**放置位置**: `components/header.tsx` 或 `components/sidebar.tsx`

**步骤 1**: 创建 `components/streak-badge.tsx`
```typescript
// components/streak-badge.tsx
import { Flame } from "lucide-react";

interface StreakBadgeProps {
  streak: number;
}

export function StreakBadge({ streak }: StreakBadgeProps) {
  if (streak === 0) return null;

  return (
    <div className="flex items-center gap-1 text-orange-500">
      <Flame className="w-5 h-5" />
      <span className="font-bold">{streak}</span>
    </div>
  );
}
```

**步骤 2**: 在 Header 合适位置导入
```typescript
// components/header.tsx
import { StreakBadge } from "./streak-badge";

// 在用户信息附近添加
<StreakBadge streak={userProgress.streak} />
```

---

## P0 — 数据库迁移

### LING-P0-DB-MIGRATION · 同步新表到数据库

**现状**: 以下表已定义但未创建：
- `db/schema-word-progress.ts` (userWords, challengeWords, wordReviewLog)
- `db/schema-tenant.ts` (tenants, tenantMembers, classrooms, etc.)

**修复**: 运行 drizzle push

```bash
# 1. 预览变更
pnpm db:studio

# 2. 同步到数据库
pnpm db:push
```

**验证**:
```bash
# 检查表是否创建
psql $DATABASE_URL -c "\dt" | grep -E "user_words|tenants|classrooms"
```

---

## P1 — 好友系统

### LING-P1-FRIENDS-1 · 好友数据模型

**修复**: 创建 `db/schema-social.ts`

```typescript
// db/schema-social.ts
import { relations } from "drizzle-orm";
import {
  boolean,
  integer,
  pgTable,
  primaryKey,
  serial,
  text,
  timestamp,
} from "drizzle-orm/pg-core";
import { userProgress } from "./schema";

export const friendships = pgTable(
  "friendships",
  {
    id: serial("id").primaryKey(),
    requesterId: text("requester_id")
      .references(() => userProgress.userId, { onDelete: "cascade" })
      .notNull(),
    addresseeId: text("addressee_id")
      .references(() => userProgress.userId, { onDelete: "cascade" })
      .notNull(),
    status: text("status").notNull().default("pending"), // pending | accepted | rejected
    createdAt: timestamp("created_at").notNull().defaultNow(),
  },
  (table) => ({
    pk: primaryKey({ columns: [table.requesterId, table.addresseeId] }),
  })
);

export const friendshipsRelations = relations(friendships, ({ one }) => ({
  requester: one(userProgress, {
    fields: [friendships.requesterId],
    references: [userProgress.userId],
    relationName: "requester",
  }),
  addressee: one(userProgress, {
    fields: [friendships.addresseeId],
    references: [userProgress.userId],
    relationName: "addressee",
  }),
}));
```

### LING-P1-FRIENDS-2 · 好友 API

**修复**: 创建 `app/api/friends/route.ts`

```typescript
// app/api/friends/route.ts
import { NextResponse } from "next/server";
import { auth } from "@clerk/nextjs/server";
import { eq, and, or } from "drizzle-orm";

import db from "@/db/drizzle";
import { friendships } from "@/db/schema-social";

// GET - 获取好友列表
export async function GET() {
  const { userId } = await auth();
  if (!userId) return NextResponse.json({ error: "Unauthorized" }, { status: 401 });

  const friends = await db.query.friendships.findMany({
    where: and(
      or(
        eq(friendships.requesterId, userId),
        eq(friendships.addresseeId, userId)
      ),
      eq(friendships.status, "accepted")
    ),
  });

  return NextResponse.json({ friends });
}

// POST - 发送好友请求
export async function POST(request: Request) {
  const { userId } = await auth();
  if (!userId) return NextResponse.json({ error: "Unauthorized" }, { status: 401 });

  const { addresseeId } = await request.json();
  if (!addresseeId) return NextResponse.json({ error: "addresseeId required" }, { status: 400 });

  // 检查是否已存在
  const existing = await db.query.friendships.findFirst({
    where: or(
      and(eq(friendships.requesterId, userId), eq(friendships.addresseeId, addresseeId)),
      and(eq(friendships.requesterId, addresseeId), eq(friendships.addresseeId, userId))
    ),
  });

  if (existing) return NextResponse.json({ error: "Already exists" }, { status: 400 });

  await db.insert(friendships).values({
    requesterId: userId,
    addresseeId,
  });

  return NextResponse.json({ success: true }, { status: 201 });
}
```

### LING-P1-FRIENDS-3 · 好友页面

**修复**: 创建 `app/(main)/friends/page.tsx`

```
/friends
  显示: 好友列表、请求管理、搜索用户
```

---

## P2 — 收尾

### LING-P2-CLEANUP-1 · 更新 drizzle.ts

确保所有 schema 被加载

```typescript
// db/drizzle.ts
import * as schema from "./schema";
import * as schemaWordProgress from "./schema-word-progress";
import * as schemaTenant from "./schema-tenant";
import * as schemaSocial from "./schema-social";  // 新增

const db = drizzle(sql, {
  schema: { ...schema, ...schemaWordProgress, ...schemaTenant, ...schemaSocial },
});
```

### LING-P2-CLEANUP-2 · 更新 README

记录 v1.2.0-v1.8.0 所有变更

---

## 立即行动清单

| 任务 | 预计 | 优先级 | 状态 |
| ---- | ---- | ------ | ---- |
| 创建 StreakToast 组件 | 1h | P0 | ⏳ |
| 修改 Learn 页面调用 | 0.5h | P0 | ⏳ |
| 创建 StreakBadge 组件 | 0.5h | P0 | ⏳ |
| 添加到 Header | 0.5h | P0 | ⏳ |
| 运行 db:push | 1h | P0 | ⏳ |
| 创建 schema-social.ts | 1h | P1 | ⏳ |
| 实现好友 API | 2h | P1 | ⏳ |
| 创建好友页面 | 2h | P1 | ⏳ |
| 更新 drizzle.ts | 0.5h | P2 | ⏳ |

---

## 版本历史

| 版本 | 日期 | 分数 | 变更 |
| ---- | ---- | ---- | ---- |
| v1.2.0 | 2026-07-02 | ✅ | verify 真实化 + Stripe + PostHog |
| v1.4.0 | 2026-07-02 | ✅ | PWA 离线 + manifest |
| v1.5.0 | 2026-07-02 | ✅ | B2B 多租户 + 教师 Dashboard |
| v1.6.0 | 2026-07-02 | ✅ | 自适应学习 + AI 发音 |
| v1.7.0 | 2026-07-02 | ✅ | 中间件合并 + 基础设施 |
| **v1.8.0** | **2026-07-02** | **Target 8.0** | 签到 UI + 好友 + 数据库迁移 |
