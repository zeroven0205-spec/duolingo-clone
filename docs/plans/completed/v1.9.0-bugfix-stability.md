# v1.9.0 — Bug Fixes + Stability

**Status:** Active
**Target Score:** 7.5 → 8.0/10
**Created:** 2026-07-02
**Based on:** [lingo-report.html](../../WorkBuddy/2026-07-02-09-00-39/lingo-report.html)

---

## 背景

深度测试报告发现 4 个关键 Bug 需要修复：

| Bug | 描述 | 优先级 |
| ---- | ---- | ------ |
| **BUG-002** | i18n 语言切换无效（EN 按钮切换后仍显示中文） | 🔴 P0 |
| **BUG-003** | /privacy 和 /terms 被登录保护（应公开） | 🔴 P0 |
| **BUG-004** | Clerk Dev 模式弹窗暴露在 UI | 🟡 P1 |
| **BUG-005** | 语言选择按钮无明确反馈 | 🟡 P1 |

---

## P0 — 紧急修复

### LING-P0-BUG-003 · /privacy 和 /terms 路由公开

**问题**: 法律页面需要登录才能访问

**修复**: 在 `middleware.ts` 中添加路由

```typescript
// middleware.ts
const isPublicRoute = createRouteMatcher([
  "/",
  "/sign-in(.*)",
  "/sign-up(.*)",
  "/api/webhooks/stripe",
  "/privacy",           // 新增
  "/privacy-policy",    // 新增
  "/terms",             // 新增
  "/terms-of-service",  // 新增
]);
```

**实施**: 只需修改 `middleware.ts` line 8-13

---

### LING-P0-BUG-002 · i18n 语言切换修复

**问题分析**:

现有实现有两套独立的 locale 系统：

| 文件 | Hook | 数据源 |
| ---- | ---- | ------ |
| `components/locale-provider.tsx` | `useTranslation()` | Context |
| `components/use-locale.ts` | `useLocale()` | localStorage |

**根本原因**: `LocaleLoader` 切换时只更新了 Context，但组件可能未正确订阅

**修复方案**:

```typescript
// 方案：统一使用 Context，确保切换时强制刷新
// components/locale-loader.tsx

function switchLocale(newLocale: Locale) {
  localStorage.setItem("locale", newLocale);
  setLocale(newLocale);
  localeModules[newLocale]().then((m) => {
    setMessages(m);
    // 关键：触发页面刷新以应用新语言
    window.location.reload();
  });
}
```

**或者更简单的方案**: 页面刷新

---

### LING-P0-BUG-001 · Demo 账号入口

**问题**: Clerk 500 用户上限导致无法注册

**修复方案**: 创建静态 Demo 页面

```typescript
// app/demo/page.tsx
// 预填充的测试账号数据，无需 Clerk 登录

export default function DemoPage() {
  // 使用静态 mock 数据展示完整功能流程
  const demoUser = {
    name: "Demo User",
    streak: 7,
    points: 1250,
    hearts: 4,
  };

  return <LearnPage mockUser={demoUser} />;
}
```

---

## P1 — 体验优化

### LING-P1-BUG-004 · Clerk Dev 弹窗

**问题**: 右下角出现 Clerk 开发模式配置弹窗

**分析**: 这是 Clerk SDK 在 development 模式下的行为

**解决方案**:
1. 使用正式环境变量（生产环境不会有弹窗）
2. 或添加 CSS 隐藏：

```css
/* globals.css */
.clerk-dev-modal {
  display: none !important;
}
```

### LING-P1-BUG-005 · 语言选择按钮反馈

**修复**: 首页语言按钮点击后添加 Toast 提示

```typescript
// app/page.tsx
import { toast } from "sonner";

function selectLanguage(lang: string) {
  // 存储选中的语言
  localStorage.setItem("selectedLanguage", lang);
  toast(`已选择 ${lang}`, { icon: "🎯" });
}
```

---

## P2 — 稳定性

### LING-P2-ERROR-1 · Error Boundary

```typescript
// app/error.tsx
// 全局错误边界
```

---

## 实施清单

| 任务 | 预计 | 优先级 | 状态 |
| ---- | ---- | ------ | ---- |
| 修复 /privacy, /terms 路由 | 0.5h | 🔴 P0 | ⏳ |
| 修复 i18n 语言切换 | 1h | 🔴 P0 | ⏳ |
| Demo 账号入口 | 2h | 🔴 P0 | ⏳ |
| 隐藏 Clerk Dev 弹窗 | 1h | 🟡 P1 | ⏳ |
| 语言选择按钮反馈 | 1h | 🟡 P1 | ⏳ |

---

## 版本历史

| 版本 | 日期 | 分数 | 变更 |
| ---- | ---- | ---- | ---- |
| v1.8.0 | 2026-07-02 | ✅ | 签到 UI + 好友系统 |
| **v1.9.0** | **2026-07-02** | **Target 8.0** | Bug 修复 + 稳定性 |
