# v1.4.0 — PWA 离线支持 + 安全加固

**Status:** Active
**Target Score:** 8.5/10
**Created:** 2026-07-01
**Based on:** [v1.2.0-verify-stripe.md](./v1.2.0-verify-stripe.md)

---

## 背景

v1.3.0 稳定在 8.4/10，剩余薄弱维度：

| 维度 | 当前分 | 目标 | 差距 |
| ---- | ------ | ---- | ---- |
| security | 6.0 | 8.0 | 2个 hardcoded secret matches |
| operations | 6.5 | 8.0 | 需国内合规部署配置 |

---

## P0 阻塞项（必须修复）

### LING-P0-SEC-1 · Hardcoded Secret 清理

**证据**: 2个 hardcoded secret matches（.env.example 模板密钥残留）

**修复**:
```bash
# 确认所有 .env.* 文件在 .gitignore 中
grep -c "sk_test_\|pk_test_" .env.example
# 输出应为 0（仅占位符）
# 确认无真实密钥硬编码
grep -rnE "sk_live_|sk-[A-Za-z0-9]{24,}" --include="*.ts" --include="*.tsx"
```

### LING-P0-SEC-2 · Stripe Webhook Secret 验证

**证据**: Webhook route 无 secret 验证，存在重放攻击风险

**修复**:
```typescript
// app/api/webhooks/stripe/route.ts
import { headers } from 'next/headers';

export async function POST(req: Request) {
  const body = await req.text();
  const sig = headers().get('stripe-signature')!;

  let event: Stripe.Event;
  try {
    event = stripe.webhooks.constructEvent(
      body, sig, process.env.STRIPE_WEBHOOK_SECRET!
    );
  } catch (err) {
    return new Response('Webhook Error', { status: 400 });
  }

  // 处理事件...
}
```

---

## P1 功能项（本周完成）

### LING-P1-PWA-1 · PWA 离线缓存

**目标**: 用户可在无网环境下访问已学习内容

**技术方案**:
```typescript
// next.config.ts
const withPWA = (await import('@ducanh2912/next-pwa')).default({
  dest: 'public',
  disable: process.env.NODE_ENV === 'development',
  register: true,
  skipWaiting: true,
  runtimeCaching: [
    {
      urlPattern: /^https:\/\/fonts\.(?:gstatic|googleapis)\.com/,
      handler: 'CacheFirst',
      options: { cacheName: 'google-fonts', expiration: { maxEntries: 10 } },
    },
    {
      urlPattern: /\/api\/user-progress/,
      handler: 'NetworkFirst',
      options: { cacheName: 'user-data' },
    },
  ],
});
```

**缓存策略**:
| 资源类型 | 策略 | TTL |
| -------- | ---- | --- |
| 静态资源 (JS/CSS) | CacheFirst | 7天 |
| 课程数据 API | StaleWhileRevalidate | 1小时 |
| 用户进度 | NetworkFirst | 即时 |
| 图片资源 | CacheFirst | 30天 |

### LING-P1-PWA-2 · Service Worker 更新提示

**目标**: 检测到新版本时提示用户刷新

```typescript
// components/update-toast.tsx
// 当检测到 SW 更新时显示「有新版本可用，点击刷新」toast
```

---

## P2 体验优化（1-2周）

### LING-P2-STREAK-1 · 连续签到激励机制

**目标**: 提升 DAU 和留存率

**机制设计**:
- 连续签到 3 天 → 奖励 10 积分
- 连续签到 7 天 → 奖励 1 红心
- 连续签到 30 天 → 专属徽章 + 1 个月 Plus 免费

**数据模型变更**:
```typescript
// 新增字段
streak: integer("streak").default(0),        // 当前连续天数
lastLoginDate: timestamp("last_login_date"),   // 上次登录日期
streakHistory: jsonb("streak_history"),        // 历史记录
```

### LING-P2-LANG-1 · 多语言 UI 扩展

**当前**: 仅中英文
**目标**: 日语、韩语 UI（对应课程语言）

```json
// messages/ja.json（新增）
// messages/ko.json（新增）
```

---

## 立即行动清单

| 任务 | 预计 | 优先级 | 状态 |
| ---- | ---- | ------ | ---- |
| 清理 hardcoded secrets | 0.5h | P0 | ⏳ |
| Stripe webhook 签名验证 | 1h | P0 | ⏳ |
| PWA 配置（next-pwa） | 3h | P1 | ⏳ |
| Service Worker 更新提示 | 1h | P1 | ⏳ |
| 连续签到数据模型 | 2h | P2 | ⏳ |

---

## 版本历史

| 版本 | 日期 | 分数 | 变更 |
| ---- | ---- | ---- | ---- |
| v1.0.0 | 2026-07-01 | 7.9/10 | 商业化基线 |
| v1.1.0 | 2026-07-01 | 8.0/10 | i18n + SEO |
| v1.2.0 | 2026-07-01 | 8.4/10 | verify + docs 完善 |
| v1.3.0 | 2026-07-01 | 8.4/10 | 稳定 8.4 |
| v1.4.0 | 2026-07-01 | Target 8.5 | PWA离线 + 安全加固 |
