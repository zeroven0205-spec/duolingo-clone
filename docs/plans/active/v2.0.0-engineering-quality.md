# v2.0.0 — Engineering Quality + Testing

**Status:** Active
**Target Score:** 5.56 → 7.0/10
**Created:** 2026-07-02

---

## 背景

深度测试发现工程化维度仍有问题：

| 问题 | 当前状态 | 优先级 |
| ---- | -------- | ------ |
| verify.sh 用错误的命令 | `pnpm test --run` 无效 | 🔴 P0 |
| ESLint config 损坏 | circular JSON 报错 | 🔴 P0 |
| 零测试覆盖 | 无任何 .test.ts 文件 | 🔴 P1 |

---

## P0 — 立即修复

### LING-P0-VERIFY · verify.sh 包管理器修复

**问题**: 项目用 pnpm，但 verify.sh 用 `pnpm test --run`，vitest 不识别 `--run`

**修复**: 改用 `pnpm exec vitest --run`

```bash
# 原来
pnpm test --run

# 修改为
pnpm exec vitest --run
# 或直接
vitest --run
```

**验证**:
```bash
bash scripts/verify.sh
# 应该实际运行 vitest
```

---

### LING-P0-ESLINT · ESLint Config 修复

**问题**: `eslint.config.mjs` circular JSON 报错

**分析**: FlatCompat + React 插件导致循环引用

**修复**: 重写为更简单的配置

```javascript
// eslint.config.mjs
import { dirname } from "path";
import { fileURLToPath } from "url";
import eslint from "@eslint/js";
import tseslint from "typescript-eslint";
import importPlugin from "eslint-plugin-import";
import reactHooks from "eslint-plugin-react-hooks";
import globals from "globals";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

export default tseslint.config(
  { ignores: ["node_modules/**", ".next/**", "out/**", "build/**"] },
  {
    extends: [eslint.configs.recommended],
    plugins: {
      "@typescript-eslint": tseslint.plugin,
      "react-hooks": reactHooks,
      "import": importPlugin,
    },
    rules: {
      ...tseslint.configs.recommended.rules,
      ...reactHooks.configs.recommended.rules,
      "import/no-unresolved": "error",
    },
    languageOptions: {
      globals: {
        ...globals.browser,
        ...globals.node,
      },
      parserOptions: {
        ecmaFeatures: { jsx: true },
      },
    },
  }
);
```

**或更简单方案** — 删除 eslint.config.mjs，使用 Next.js 默认：

```bash
rm eslint.config.mjs
# Next.js 自带 eslint 配置
```

---

## P1 — 测试覆盖

### LING-P1-TEST-1 · 添加 Vitest 测试

**问题**: 项目零测试覆盖

**修复**: 为核心功能添加测试

**测试文件位置**: `tests/` 或 `__tests__/`

**1. Spaced Repetition 算法测试**
```typescript
// tests/spaced-repetition.test.ts
import { calculateNextReview, createWordCard } from "@/lib/spaced-repetition";

describe("Spaced Repetition", () => {
  test("correct answer moves card up", () => {
    const card = createWordCard("1", "hello", "你好");
    const result = calculateNextReview(card, true);
    expect(result.newBox).toBe(1);
    expect(result.correct).toBe(true);
  });

  test("incorrect answer resets to box 0", () => {
    const card = { ...createWordCard("1", "hello", "你好"), box: 3 };
    const result = calculateNextReview(card, false);
    expect(result.newBox).toBe(0);
    expect(result.correct).toBe(false);
  });
});
```

**2. Streak 逻辑测试**
```typescript
// tests/streak.test.ts
import { STREAK_REWARDS } from "@/actions/user-streak";

describe("Streak Rewards", () => {
  test("3 day streak gives points", () => {
    expect(STREAK_REWARDS[3]).toEqual({ type: "points", value: 10 });
  });

  test("7 day streak gives hearts", () => {
    expect(STREAK_REWARDS[7]).toEqual({ type: "hearts", value: 1 });
  });
});
```

**3. Review Queue API 测试**
```typescript
// tests/review-queue.test.ts
import { NextRequest } from "next/server";

describe("GET /api/review-queue", () => {
  test("returns words due for review", async () => {
    // Mock auth and DB
    const req = new NextRequest("http://localhost/api/review-queue");
    const res = await GET();
    expect(res.status).toBe(200);
  });
});
```

---

## P2 — AGENTS.md 扩展

### LING-P2-AGENTS · 扩展 AGENTS.md

**当前**: 仅 51 行

**目标**: 100+ 行，覆盖更多场景

```markdown
## 测试指南
- 运行测试: `pnpm test`
- 添加新测试: 在 tests/ 目录创建 .test.ts 文件
- 测试覆盖范围: unit tests for lib/, integration tests for API routes

## 部署
- Vercel: 自动部署 from main branch
- 环境变量: 见 .env.example
```

---

## 立即行动清单

| 任务 | 预计 | 优先级 | 状态 |
| ---- | ---- | ------ | ---- |
| 修复 verify.sh (pnpm exec vitest) | 15min | 🔴 P0 | ⏳ |
| 修复 ESLint config | 30min | 🔴 P0 | ⏳ |
| 添加 spaced-repetition 测试 | 30min | 🟡 P1 | ⏳ |
| 添加 streak 测试 | 30min | 🟡 P1 | ⏳ |
| 添加 review-queue 测试 | 30min | 🟡 P1 | ⏳ |
| 扩展 AGENTS.md | 1h | 🟢 P2 | ⏳ |

---

## P0/P1 — 诊断补充：治理 Gap（8维扫描 2026-07-02 新增）

> 2026-07-02 Harness-Loop-Engineer 8维诊断扫描发现以下未覆盖问题：

### LING-GAP-SEC · 🔴 Security: 硬编码密钥

**现状**: 3处 `sk_live_|sk-[A-Za-z0-9_-]{24,}` 正则匹配
**风险**: 生产密钥若提交至 Git 将造成安全事故

**修复步骤**:
1. `grep -rE "(sk_live_|sk-[A-Za-z0-9_-]{24,})" --include="*.ts" --include="*.tsx" --include="*.js" .` 定位所有匹配
2. 逐一确认识别是否为真实密钥（大部分可能是测试占位符如 `sk_test_xxxx`）
3. 将真实密钥迁移至 `.env` 并在 `.gitignore` 中排除
4. 添加 `scripts/secret-scan.sh` 或启用 [gitleaks](https://github.com/gitleaks/gitleaks)

**验证**:
```bash
# 修复后应无匹配
grep -rE "(sk_live_|sk-[A-Za-z0-9_-]{24,})" --include="*.ts" .
# 确认 .gitignore 包含 .env
cat .gitignore | grep .env
```

---

### LING-GAP-PM · 🔴 projectManagement: 文档缺失

**现状**: 缺少 `docs/plans/active/` 和规范 CHANGELOG
**现状**: 有 CHANGELOG.md 但内容单薄（仅有 Unreleased 头部）

**修复步骤**:
1. 确认 `docs/plans/active/` 目录存在且包含本规划文件
2. 补全 CHANGELOG.md — 添加 v1.9.0 及之前版本的发布记录
3. 考虑添加 `docs/quality/verification-matrix.md`（验证矩阵）

**验证**:
```bash
ls docs/plans/active/
# 应包含 v2.0.0-engineering-quality.md
cat CHANGELOG.md | grep -E "^## \[v"
# 应有版本条目
```

---

### LING-GAP-OPS · ⚠️ Operations: CI/Verify 缺口

**现状**: 无 verify 命令（`package.json` 无 `verify` script），CI 不完整
**影响**: 代码质量门禁缺失，恶意提交无法拦截

**修复步骤**:
1. 在 `package.json` 添加 `verify` script：
   ```json
   "verify": "pnpm exec vitest --run && pnpm lint"
   ```
2. 若有 GitHub Actions，确保 `.github/workflows/` 覆盖 test + lint + type-check
3. 添加 `.nvmrc` 或 `.python-version` 锁定 Node 版本

**验证**:
```bash
cat package.json | grep verify
# 应有 "verify": ...
```

---

### LING-GAP-OBS · ⚠️ Observability: 无结构化日志

**现状**: 未检测到 `pino/winston/loguru/structured logger` 引用
**影响**: 生产问题难以追踪，无请求级别可观测性

**修复步骤**:
1. 评估引入 `pino`（Node.js 推荐）或使用 Next.js 内置 `next/log`
2. 在 API routes 和关键 Service 层添加日志调用
3. 记录结构化字段：`{ level, timestamp, reqId, userId, action, duration }`

**验证**:
```bash
grep -rE "(pino|winston|loguru|structured.*log)" --include="*.ts" .
# 应有日志库引用
```

---

## 版本历史

| 版本 | 日期 | 分数 | 变更 |
| ---- | ---- | ---- | ---- |
| v1.9.0 | 2026-07-02 | ✅ | Bug 修复 + 稳定性 |
| **v2.0.0** | **2026-07-02** | **Target 7.0** | 工程化质量 + 测试 |

### 诊断记录

| 日期 | Overall | security | projectManagement | operations | observability |
| ---- | ------- | -------- | ----------------- | ---------- | ------------- |
| 2026-07-02 | 8.2 | 6 | 5 | 6.5 | 8 |
